import { SidenavHelperDirective } from './sidenav-helper.directive';

describe('SidenavHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavHelperDirective();
    expect(directive).toBeTruthy();
  });
});
