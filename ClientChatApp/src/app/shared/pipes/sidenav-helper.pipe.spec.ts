import { SidenavHelperPipe } from './sidenav-helper.pipe';

describe('SidenavHelperPipe', () => {
  it('create an instance', () => {
    const pipe = new SidenavHelperPipe();
    expect(pipe).toBeTruthy();
  });
});
